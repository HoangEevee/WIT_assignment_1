<h2 class="center">{{data.patient}}'s Health Record </h2>

<h2>Glucose Record</h2>
<p>Lower Threshold: {{data.glucose.lower}}</p>
<p>Upper Threshold: {{data.glucose.higher}}</p>
<div class="tablecontainer">
    <table class="table">
        <thead> 
            <tr>
                <th>Time</th>
                <th>Blood Glucose (mmol/L)</th> 
            </tr>
        </thead>
        <tbody>
            {{#each data.timestamp}}
            <tr>
                <td data label = "Time">{{this.time}}</td>
                <!--This red highlight for out of range does not work yet-->
                <td data label = "Blood Glucose (mmol/L)"{{#if (outOfThreshold this.glucose data.glucose.lower data.glucose.higher)}} class="red" {{/if}}>{{this.glucose}}</td> 
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>    